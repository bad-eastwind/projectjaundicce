# Python 3.12 (cloud/HPC parity). Install torch per-platform (CUDA wheel on HPC, CPU/MPS locally).
numpy
pandas
scikit-learn
pillow
opencv-python-headless
pyyaml
tqdm
matplotlib
# Deep learning (add when building model modules):
torch
torchvision
timm
